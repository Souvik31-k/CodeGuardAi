spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5432/CodeGuardDB
    username: CodeGuardUSER
    password: SOUVIK!@#$*
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: health

  endpoint:
    health:
      show-details: always
